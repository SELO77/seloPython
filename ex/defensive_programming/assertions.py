# numbers = [1.5, 2.3, 9, 0.7, -0.01, 3]
# total = 0.0
# for n in numbers:
#   assert n >=0.0, 'Value should only contain positive values'
#   total += n
# print('total:%d'%total)

def normalize_rectangle(rect):
  assert(len(rect) == 4, 'Rectangles must contain 4 coordinates')

normalize_rectangle((1, 2, 3))


def cal_average(nums):
  pass